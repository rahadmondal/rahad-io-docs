---
title: "Day 01: JavaScript শুরু এবং সেটআপ"
description: "জাভাস্ক্রিপ্ট হিস্ট্রি, এনভায়রনমেন্ট সেটআপ এবং স্ক্রিপ্ট লোডিং স্ট্র্যাটেজি নিয়ে আলোচনা।"
date: "2023-10-26"
---

# Day 01: জাভাস্ক্রিপ্ট যাত্রা শুরু (Let the Journey Begin) 🚀

হ্যালো ফিউচার প্রোগ্রামার! 👋  
স্বাগতম **Day 01**-এ। আজ আমরা কোডিংয়ের জগতে একটি বড় পদক্ষেপ নিতে যাচ্ছি। ভয় পাওয়ার কিচ্ছু নেই, আমরা খুব সহজভাবে গল্প করতে করতে শিখব জাভাস্ক্রিপ্ট আসলে কী এবং এটি দিয়ে কী জাদু করা যায়।

চলুন, কফির মগ হাতে নিয়ে শুরু করা যাক! ☕

---

## JavaScript & It's History (পেছনের গল্প) 📜

আপনি কি জানেন? জাভাস্ক্রিপ্ট ছাড়া আজকের ইন্টারনেট অচল! ফেসবুকের লাইক বাটন, ইউটিউবের ভিডিও প্লেয়ার—সবই এর কারিশমা।

- **জন্মকথা:** ১৯৯৫ সাল। নেটস্কেপ ব্রাউজারের একজন ইঞ্জিনিয়ার, নাম **ব্রেন্ডন আইক (Brendan Eich)**। তিনি মাত্র **১০ দিনে** এই ল্যাঙ্গুয়েজটি বানিয়ে ফেলেছিলেন! ভাবা যায়?
- **নাম নিয়ে বিভ্রান্তি:** শুরুতে এর নাম ছিল _Mocha_, এরপর _LiveScript_। কিন্তু তখন বাজারে **Java** ল্যাঙ্গুয়েজ খুব জনপ্রিয় ছিল। তাই অনেকটা মার্কেটিং স্টান্ট হিসেবে নাম বদলে রাখা হয় **JavaScript**।

> 💡 **মনে রাখবেন:** _Car_ এবং _Carpet_ যেমন এক জিনিস নয়, তেমনি _Java_ এবং _JavaScript_-ও এক জিনিস নয়। এরা সম্পূর্ণ আলাদা!

## Environment Setup (আমাদের যা যা লাগবে) 🛠️

জাভাস্ক্রিপ্ট শেখার জন্য আপনার সুপারকম্পিউটার লাগবে না। আপনার ল্যাপটপে শুধু দুটি জিনিস থাকলেই হবে:

1.  **Web Browser:** যেখানে আমরা আউটপুট দেখব (আমি **Google Chrome** ব্যবহারের পরামর্শ দেব)।
2.  **Code Editor:** যেখানে আমরা কোড লিখব (সেরা হলো **VS Code**)।

ব্যাস, আমরা রেডি!

## First Line of JS Code (চলুন হ্যালো বলি) 👋

কোনো সফটওয়্যার ইন্সটল ছাড়াই আমরা ব্রাউজারের সাথে কথা বলতে পারি। আসুন দেখি কিভাবে:

1.  Chrome ব্রাউজারের যেকোনো জায়গায় `Right Click` করুন এবং `Inspect`-এ যান।
2.  `Console` ট্যাবে ক্লিক করুন।
3.  নিচের লাইনটি লিখুন এবং `Enter` চাপুন:

```javascript
console.log("হ্যালো পৃথিবী! আমি জাভাস্ক্রিপ্ট শিখছি!");
```

অভিনন্দন\! 🎉 আপনি এইমাত্র আপনার জীবনের প্রথম জাভাস্ক্রিপ্ট কোড রান করেছেন।

---

## HTML এর সাথে JavaScript কানেক্ট করার উপায় 🔗

জাভাস্ক্রিপ্ট কোডকে ব্রাউজারে রান করানোর জন্য একে HTML ফাইলের সাথে লিংক বা কানেক্ট করতে হয়। এর জন্য আমরা `<script>` ট্যাগ ব্যবহার করি।

### Head Tag পদ্ধতি

সাধারণত কানেক্ট করার একটি সনাতন পদ্ধতি হলো `<head>` ট্যাগের ভেতরে স্ক্রিপ্ট ফাইল লিংক করা।

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Day 01</title>
    <script src="script.js"></script>
  </head>
  <body>
    <h1>আমার ওয়েবসাইট</h1>
  </body>
</html>
```

### সমস্যা: Render Blocking ⚠️

আমরা যখন উপরের নিয়মে `<head>` ট্যাগে স্ক্রিপ্ট কানেক্ট করি, তখন একটা ঝামেলা হয়। ব্রাউজার যখন কোড পড়ে, সে ওপর থেকে নিচে নামে। যখনই সে `<script>` ট্যাগ পায়:

1.  সে HTML পড়া থামিয়ে দেয়।
2.  আগে ওই JS ফাইলটা ডাউনলোড আর রান করতে ব্যস্ত হয়ে পড়ে।
3.  ততক্ষণ পর্যন্ত আপনার ওয়েবসাইটের `<body>` বা মূল কন্টেন্ট লোড হয় না।

এর ফলে ইউজার কিছুক্ষণ **সাদা স্ক্রিন (White Screen)** দেখতে পায়। একে টেকনিক্যাল ভাষায় বলা হয় **Render Blocking**।

### সমাধান: Body Tag পদ্ধতি 💡

এই সমস্যা এড়াতে এবং ইউজার এক্সপেরিয়েন্স ভালো করতে ডেভেলপাররা স্ক্রিপ্ট ট্যাগটি `<body>` এর একদম শেষে রাখা শুরু করলেন।

```html
<body>
  <h1>আগে লেখা দেখা যাবে</h1>
  <p>তারপর স্ক্রিপ্ট লোড হবে...</p>

  <script src="script.js"></script>
</body>
```

এতে সুবিধা হলো, ইউজার অন্তত ওয়েবসাইটটা দেখতে পেল, তারপর ব্যাকগ্রাউন্ডে জাভাস্ক্রিপ্ট লোড হলো।

---

## আধুনিক স্ক্রিপ্ট লোডিং (Async vs Defer) ⚡

এইচটিএমএল-৫ আমাদের জন্য জীবন সহজ করতে `async` এবং `defer` অ্যাট্রিবিউট এনেছে। এগুলো ব্যবহার করলে `<head>` ট্যাগে স্ক্রিপ্ট রেখেই ফাস্ট লোডিং পাওয়া সম্ভব।

### The `async` Attribute

```html
<script async src="script.js"></script>
```

- **কি হয়:** এটি ট্রেনের মতো সমান্তরালভাবে (Parallel) চলে। HTML লোড হওয়ার সাথে সাথেই ব্যাকগ্রাউন্ডে স্ক্রিপ্ট ডাউনলোড হতে থাকে।
- **সমস্যা:** ডাউনলোড শেষ হওয়া মাত্রই এটি হুট করে রান করে বসে। তখন HTML লোডিং সাময়িকভাবে আটকে যেতে পারে।
- **ব্যবহার:** সাধারণত থার্ড-পার্টি টুল (যেমন: Google Analytics) লোড করতে এটি ব্যবহার হয়।

### The `defer` Attribute (Best Practice) 🏆

সবচেয়ে নিরাপদ এবং সেরা উপায় হলো `defer` ব্যবহার করা।

```html
<script defer src="script.js"></script>
```

- **মজাটা কোথায়:** এটিও ব্যাকগ্রাউন্ডে ডাউনলোড হয়, কিন্তু ডাউনলোড শেষ হলেও সাথে সাথে রান করে না। এটি অপেক্ষা করে যতক্ষণ না পুরো HTML পেজ লোড শেষ হয়।
- **কেন সেরা:** এটি পেজ লোডিং ফাস্ট রাখে এবং কোডের অর্ডার ঠিক রাখে।

**একনজরে তুলনা:**

| পদ্ধতি     | ডাউনলোড হয়   | রান হয়           | আমাদের রেটিং |
| :--------- | :----------- | :--------------- | :----------- |
| `<script>` | লোডিং থামিয়ে | সাথে সাথে        | ❌ বাজে      |
| `async`    | সাথে সাথে    | ডাউনলোড শেষ হলেই | ⚠️ মোটামুটি  |
| `defer`    | সাথে সাথে    | সব শেষে          | ✅ সেরা      |

---

## সার্ভারে জাভাস্ক্রিপ্ট (ব্রাউজারের বাইরে মুক্তি) 🌍

২০০৯ সালের আগে জাভাস্ক্রিপ্ট ছিল খাঁচায় বন্দী পাখির মতো—শুধুমাত্র ব্রাউজারের ভেতরেই চলত।

২০০৯ সালে **Ryan Dahl** ভাবলেন, "আচ্ছা, এটাকে যদি কম্পিউটারেও চালানো যেত?" তিনি ক্রোম ব্রাউজারের ইঞ্জিন (V8 Engine) বের করে আনলেন এবং বানালেন **Node.js**।

এর ফলে জাভাস্ক্রিপ্ট এখন শুধু ওয়েবসাইট ডিজাইনে নয়, বড় বড় সার্ভার এবং সফটওয়্যার বানাতেও ব্যবহৃত হয়।

টার্মিনালে কোড রান করতে চাইলে:

```bash
node index.js
```

---

🎉 **শাবাশ\!** আপনি আজ অনেক গুরুত্বপূর্ণ থিওরি শিখে ফেলেছেন। আজ এ পর্যন্তই। কাল আমরা কোডিংয়ের গভীরে ডুব দেব। দেখা হচ্ছে **Day 02**-তে\!
